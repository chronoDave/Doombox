language: node_js
services:
  - xvfb
node_js:
  - lts/*
cache: yarn

jobs:
  include:
    - stage: Test
      script:
        - yarn
        - yarn test

stages:
  - name: Test
    if: type = push OR type = pull_request
