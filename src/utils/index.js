const capitalize = require('./capitalize');
const clamp = require('./clamp');
const generateUid = require('./generateUid');
const getCumulative = require('./getCumulative');
const getLevenshteinDistance = require('./getLevenshteinDistance');
const localizeTag = require('./localizeTag');
const pascalize = require('./pascalize');
const shuffle = require('./shuffle');
const sortMetadata = require('./sortMetadata');
const toArray = require('./toArray');

module.exports = {
  capitalize,
  clamp,
  generateUid,
  getCumulative,
  getLevenshteinDistance,
  localizeTag,
  pascalize,
  shuffle,
  sortMetadata,
  toArray
};
