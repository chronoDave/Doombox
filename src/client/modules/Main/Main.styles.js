import { makeStyles } from '../../theme';

export default makeStyles(() => ({
  root: {
    display: 'flex',
    minHeight: 0
  },
  player: {
    display: 'flex',
    flexDirection: 'column'
  }
}), 'main');
