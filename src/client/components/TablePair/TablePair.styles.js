import { makeStyles } from '../../theme';

export default makeStyles(theme => ({
  root: {
    margin: 0,
    borderSpacing: 0
  },
  label: {
    padding: 0,
    paddingRight: theme.spacing(2)
  }
}), 'tablePair');
