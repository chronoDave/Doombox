import createStyles from '@material-ui/core/styles/createStyles';

const SideMenuButtonsStyle = theme => createStyles({
  root: {
    color: theme.palette.grey[0]
  }
});

export default SideMenuButtonsStyle;
