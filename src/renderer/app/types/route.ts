export enum App {
  Load = 'load',
  Scan = 'scan',
  Home = 'home',
  Settings = 'settings'
}

export enum Home {
  Library = 'library',
  Playlist = 'playlist',
  Search = 'search'
}

export enum Search {
  Label = 'label',
  Album = 'album',
  Song = 'song'
}

export enum Settings {
  Appearance = 'appearance',
  Library = 'library'
}
