export enum App {
  Load = 'load',
  Scan = 'scan',
  Home = 'home'
}

export enum Search {
  Label = 'label',
  Album = 'album',
  Song = 'song'
}

export enum Home {
  Library = 'library',
  Playlist = 'playlist'
}
