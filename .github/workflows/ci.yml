name: doombox
on: [push]
env:
  WEBHOOK_URL: ${{secrets.WEBHOOK_URL}}
jobs:
  test:
    name: ${{matrix.os}}
    runs-on: ${{matrix.os}}
    env:
      WEBHOOK_FOOTER: ${{matrix.os}}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    steps:
      - name: Checkout
        uses: actions/checkout@v2.3.4
      - name: Get yarn cache directory path
        id: yarn-cache-dir-path
        run: echo "::set-output name=dir::$(yarn cache dir)" 
      - name: Cache node_modules
        uses: actions/cache@v2.1.3
        id: yarn-cache
        with:
          path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
          key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
          restore-keys: |
            ${{ runner.os }}-yarn-
      - name: Install Node
        uses: actions/setup-node@v2.1.3
      - name: Install Packages
        run: yarn --frozen-lockfile
      - name: Lint
        run: yarn lint
      - name: Test
        run: yarn test
      - name: Build
        run: yarn build
      - name: On success
        if: success()
        run: |
          curl https://raw.githubusercontent.com/chronoDave/user-scripts/master/webhooks/discord_github.sh --output webhook.sh
          chmod +x webhook.sh
          ./webhook.sh success
      - name: On error
        if: failure()
        run: |
          curl https://raw.githubusercontent.com/chronoDave/user-scripts/master/webhooks/discord_github.sh --output webhook.sh
          chmod +x webhook.sh
          ./webhook.sh failure
