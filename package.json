{
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "bump:patch": "yarn version --patch && yarn workspaces version --patch",
    "bump:minor": "yarn version --minor && yarn workspaces version --minor",
    "bump:major": "yarn version --major && yarn workspaces version --major"
  },
  "devDependencies": {},
  "dependencies": {},
  "workspaces": [
    "packages/*"
  ]
}
