const CACHE = require('./src/cache');
const CONFIG = require('./src/config');
const THEME = require('./src/theme');
const {
  TYPES,
  STATUS,
  URLS,
  EVENTS,
  MENUS,
  IPC,
  ROUTES
} = require('./src/types');

module.exports = {
  CACHE,
  CONFIG,
  THEME,
  TYPES,
  STATUS,
  URLS,
  EVENTS,
  MENUS,
  IPC,
  ROUTES
};
