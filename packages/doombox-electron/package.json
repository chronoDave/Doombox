{
  "name": "@doombox/electron",
  "version": "2.0.0",
  "author": "Chronocide",
  "private": true,
  "license": "GPL-3.0",
  "main": "src/index.js",
  "scripts": {
    "start": "nodemon --config .nodemon.json",
    "test": "ava src/**/*.spec.js",
    "clean": "node scripts/clean.js",
    "build": "yarn clean && webpack --mode production && electron-builder -c .build.config.js",
    "build:portable": "yarn clean && webpack --mode production && cross-env NODE_ENV=portable electron-builder -c .build.config.js"
  },
  "dependencies": {
    "@doombox/utils": "./packages/doombox-utils",
    "fast-glob": "^3.2.4",
    "fs-extra": "^9.0.1",
    "leaf-db": "^1.0.9",
    "lodash.debounce": "^4.0.8",
    "lodash.get": "^4.4.2",
    "lodash.set": "^4.3.2",
    "music-metadata": "^7.0.2"
  },
  "devDependencies": {
    "chokidar": "^3.4.2",
    "cross-env": "^7.0.2",
    "electron": "10.1.0",
    "electron-builder": "^22.8.0",
    "fs-webpack-plugin": "^2.0.0",
    "nodemon": "^2.0.4",
    "webpack": "^4.44.1",
    "webpack-cli": "^3.3.12"
  }
}
