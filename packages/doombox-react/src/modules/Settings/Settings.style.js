import { makeStyles } from '@material-ui/core/styles';

export const useSettingsStyle = makeStyles(() => ({
  tabsRoot: {
    width: 'fit-content',
    flexShrink: 0
  },
  tabRoot: {
    opacity: 0.4
  }
}));
